" Filetype plugin, required to call after the packadd commands
filetype plugin indent on

" Load local changes
if filereadable(expand("~/.local/share/vim/vimrc"))
    source ~/.local/share/vim/vimrc
endif
